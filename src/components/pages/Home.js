import React from "react";
import { Container } from "react-bootstrap";

const Home = props => {
    return (
        <>
            <Container>
                <h1>Welcome to expense Tracker</h1>
            </Container>
        </>
    );
};

export default Home;